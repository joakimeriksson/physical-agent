[project]
name = "lab4-full-agent"
version = "0.1.0"
description = "Smart Home Agent - Python server + browser UI"
requires-python = ">=3.12"
dependencies = [
    "starlette>=0.40",
    "uvicorn>=0.30",
    "httpx>=0.27",
]
